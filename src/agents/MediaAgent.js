class MediaAgent {
  constructor() {
    // placeholder for media-related tasks (audio, video, images)
  }
  // stub API surface for future integration
  processMedia(task) {
    return { status: 'not_implemented', task };
  }
}

module.exports = MediaAgent;

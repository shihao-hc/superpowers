class MediaAgent {
  async download(url, format) {
    // Placeholder: in phase 1, delegate to existing API or return simulated result
    return `准备下载媒体：${url}，格式：${format}`;
  }
}

module.exports = new MediaAgent();

QA Validation Plan (Phase 7+)
- Verify key flows: personality, memory, routing, inference bridge, and plugin hooks.
- Run npm test, npm run learn-eval, and CI workflow will run automatically on push.
- Manual checks: health, /api/infer, plugin loading, and basic end-to-end via CLI and frontend.
